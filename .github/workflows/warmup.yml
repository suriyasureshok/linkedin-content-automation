name: Warmup Render

on:
  schedule:
    - cron: "25 3 * * 1"

jobs:

  warmup:

    runs-on: ubuntu-latest

    steps:

      - name: Warm Render
        run: |
          curl https://YOUR_RENDER_URL.onrender.com/health
          